export interface ContextStories {
  altAccount: boolean;
  full: boolean;
}

export interface ContextFeeds {
  altAccount: boolean;
  feedDays: number;
  full: boolean;
}

export interface ContextProfiles {
  altAccount: boolean;
  full: boolean;
  highlights: boolean;
  igtv: boolean;
  stories: boolean;
  tagged: boolean;
}
